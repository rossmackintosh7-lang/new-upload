import { json, requireAdmin, ensurePbiOpsTables } from './_shared.js';
async function all(env,sql){try{return (await env.DB.prepare(sql).all()).results||[]}catch{return[]}}
export async function onRequestGet({request,env}){const {response}=await requireAdmin(env,request); if(response)return response; await ensurePbiOpsTables(env); const notifications=await all(env,`SELECT status,COUNT(*) count FROM admin_notifications GROUP BY status`); const requests=await all(env,`SELECT status,COUNT(*) count FROM admin_requests GROUP BY status`); const latest_notifications=await all(env,`SELECT * FROM admin_notifications ORDER BY datetime(created_at) DESC LIMIT 12`); const latest_requests=await all(env,`SELECT * FROM admin_requests ORDER BY datetime(created_at) DESC LIMIT 12`); return json({ok:true,notifications,requests,latest_notifications,latest_requests});}
