# PBI_READY full build

This is the fresh full Purbeck Business Innovations build for Cloudflare Pages and GitHub repo `rossmackintosh7-lang/new-upload`.

## Upload target
- Upload the contents of this folder to the repo root.
- `wrangler.toml` is included and currently uses `pages_build_output_dir = "."`.
- Cloudflare Pages Functions live in `/functions`.
- Static site routes live from the project root, including `/builder/`, `/dashboard/`, `/seo-agent/`, `/analytics/`, `/admin/` and `/platform-command-centre/`.

## Included latest direction
- Premium customer dashboard / command centre
- Package-first builder flow
- Builder package upgrade flow
- AI site generation panel and endpoint
- SEO agent panel and endpoint
- Analytics panel and project-safe summary endpoint
- Live preview quick-edit polish
- Build free, pay only at publish
- Logo corrected back inside the banner and smaller
