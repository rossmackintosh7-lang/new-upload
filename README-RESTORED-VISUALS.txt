This rebuild restores the original front-end visual files (home, templates, example pages and styling references)
from the uploaded new-upload.zip so the agreed template imagery and current logo are kept in place.
The dashboard/admin/projects/package-gating fixes from the clean build are retained.
Examples index redirects to /templates/ so the browsing flow stays smoother.
